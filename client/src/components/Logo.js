const Logo = () => {
  return (
    <h1
      className="logo"
      style={{
        fontSize: "50px",
        fontFamily: "revert",
        color: "var(--primary-700)",
        fontWeight: "550",
        marginTop: "25px",
      }}
    >
      JobHelp
    </h1>
    // <img src={logo} className="logo" />
  );
};
export default Logo;
